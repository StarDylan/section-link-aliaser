# Section Link Aliaser

A lightweight Obsidian plugin that improves the readability of section links.

When the plugin encounters a link like `[[Note#Section]]`it automatically rewrites it as `[[Note#Section|Section]]`
